import { hasDatabase, prisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function SearchRunsPage() { const runs = hasDatabase() ? await prisma.searchRun.findMany({ orderBy: { startedAt: "desc" }, take: 50 }) : []; return <div className="page-shell"><p className="eyebrow">Discovery operations</p><h1 className="page-title">Search runs</h1><p className="page-copy">See what each manual or scheduled search found, reused, and added.</p><section className="mt-7 overflow-hidden rounded-2xl border border-white/8 bg-[#111]"><div className="overflow-x-auto"><table className="w-full min-w-[780px] text-left text-sm"><thead className="text-[11px] uppercase tracking-wider text-zinc-600"><tr>{["Query", "Status", "Found", "Duplicates", "New", "Quality", "Started"].map((item) => <th key={item} className="px-5 py-3">{item}</th>)}</tr></thead><tbody>{runs.map((run) => <tr key={run.id} className="border-t border-white/6 text-zinc-400"><td className="px-5 py-4 font-medium text-white">{run.queryText}</td><td className="px-5 py-4">{run.status}</td><td className="px-5 py-4">{run.businessesFound}</td><td className="px-5 py-4">{run.duplicateCount}</td><td className="px-5 py-4">{run.newLeadCount}</td><td className="px-5 py-4">{run.highCount} H · {run.mediumCount} M · {run.lowCount} L</td><td className="px-5 py-4">{run.startedAt.toLocaleString()}</td></tr>)}{!runs.length && <tr><td colSpan={7} className="px-5 py-14 text-center text-zinc-600">No live searches yet. Connect the database, then run your first search.</td></tr>}</tbody></table></div></section></div>; }
